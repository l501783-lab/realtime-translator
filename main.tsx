import React,{useMemo,useState}from"react";import{createRoot}from"react-dom/client";import{LineChart,Line,XAxis,YAxis,CartesianGrid,Tooltip,ResponsiveContainer}from"recharts";import"./style.css";
type R={year:number,return:number};type A={id:string,name:string,ticker:string,start:number,end:number,source:string,rows:R[]};
const demo=(id:string,name:string,ticker:string,start:number,vals:number[],source:string):A=>({id,name,ticker,start,end:start+vals.length-1,source,rows:vals.map((x,i)=>({year:start+i,return:x}))});
/* 開發測試資料：正式版由 GitHub Actions 下載並寫入 public/market-data/*.json 後覆蓋。 */
const assets:A[]=[
demo("SP500","S&P 500","^GSPC",1990,[-.0616,.267,.0446,.0706,-.0154,.3411,.2026,.3336,.2858,.2104,-.091,-.1189,-.221,.2861,.1088,.0491,.1579,.0549,-.37,.2646,.1506,.0211,.1589,.3215,.135,.0138,.1196,.2183,-.0438,.3149,.184,.2871,-.1811,.2423,.2364,.25],"Yahoo Finance / 年度收盤價（示範）"),
demo("DOW","道瓊指數","^DJI",1990,Array(36).fill(0),"Yahoo Finance（正式版 Actions 下載）"),
demo("GLOBAL","全球指數","ACWI",2008,Array(18).fill(0),"MSCI/ACWI 代理資料；正式版標示實際可回測年份"),
demo("TWII","台灣加權指數","^TWII",1990,[-.5293,.1044,-.0776,.367,-.0792,.057,.277,.194,-.217,.31,-.435,-.237,.107,.312,.04,.065,.24,.082,-.462,.783,.091,-.212,.088,.11,.085,-.107,.108,.154,-.085,.227,.224,.233,-.225,.27,.286,.2574],"TWSE/TIP；示範年度資料"),
demo("NIKKEI","日本指數","^N225",1990,Array(36).fill(0),"Yahoo Finance / Nikkei 225（正式版 Actions 下載）"),
demo("NASDAQ","Nasdaq Composite","^IXIC",1990,[-.1725,.567,.152,.145,-.03,.396,.226,.216,.399,.859,-.391,-.216,-.329,.503,.085,.016,.095,.093,-.406,.436,.169,-.018,.159,.383,.135,.054,.09,.286,-.041,.354,.435,.213,-.335,.433,.289,.20],"Yahoo Finance / 年度收盤價（示範）"),
demo("GOLD","黃金現貨","GC=F",1990,Array(36).fill(0),"Yahoo Finance COMEX Gold；正式版 Actions 下載"),
demo("TSMC","台積電","2330.TW",1994,Array(32).fill(0),"TWSE / Yahoo Finance；1994 起"),
demo("0050","元大台灣50","0050.TW",2003,Array(23).fill(0),"TWSE / Yahoo Finance；ETF 成立後才可回測"),
demo("0056","元大高股息","0056.TW",2007,Array(19).fill(0),"TWSE / Yahoo Finance；ETF 成立後才可回測"),
demo("00713","元大台灣高息低波","00713.TW",2017,Array(9).fill(0),"TWSE / Yahoo Finance；ETF 成立後才可回測")
];
const fmt=(n:number)=>new Intl.NumberFormat("zh-TW",{maximumFractionDigits:1}).format(n);
function App(){const[a,setA]=useState("SP500"),[s,setS]=useState(1990),[e,setE]=useState(2025),[p,setP]=useState(1000);const asset=assets.find(x=>x.id===a)!;const lo=Math.max(1990,asset.start),hi=Math.min(2025,asset.end);const rows=useMemo(()=>asset.rows.filter(x=>x.year>=s&&x.year<=e),[asset,s,e]);const sim=useMemo(()=>{let v=p;return rows.map(r=>{const before=v,gain=v*r.return;v+=gain;return{year:r.year,return:r.return,before,gain,value:v}})},[rows,p]);const final=sim.length?sim.at(-1)!.value:p,yrs=sim.length,cagr=yrs&&p>0?Math.pow(final/p,1/yrs)-1:0,best=sim.length?sim.reduce((x,y)=>y.return>x.return?y:x):null,worst=sim.length?sim.reduce((x,y)=>y.return<x.return?y:x):null;return <><header><span>B MODE · 歷史實際回測</span><h1>投資歷史回測</h1><p>選擇標的、年份與本金，逐年套用歷史報酬率</p></header><main><section className="card"><h2>回測設定</h2><label>投資標的<select value={a}onChange={x=>{setA(x.target.value);setS(Math.max(1990,assets.find(y=>y.id===x.target.value)!.start))}}>{assets.map(x=><option value={x.id}>{x.name} · {x.ticker}</option>)}</select></label><div className="grid"><label>起始年份<input type="number"min={lo}max={hi}value={s}onChange={x=>setS(+x.target.value)}/></label><label>結束年份<input type="number"min={lo}max={hi}value={e}onChange={x=>setE(+x.target.value)}/></label></div><label>初始本金（萬元）<input type="number"min="0"value={p}onChange={x=>setP(+x.target.value)}/></label><div className="info"><b>可用歷史範圍：</b>{lo}–{hi}<br/><b>資料來源：</b>{asset.source}</div><small>注意：目前專案內建的部分標的是「資料接入示範」。正式版 GitHub Actions 會依 ticker 抓取歷史價格，並對成立較晚的 ETF 自動限制起始年份；不要把不存在的年份補成 0%。</small></section><section className="metrics"><div><small>最終資產</small><b>{fmt(final)} 萬</b></div><div><small>CAGR</small><b>{(cagr*100).toFixed(2)}%</b></div><div><small>最佳年度</small><b>{best?`${best.year} ${(best.return*100).toFixed(1)}%`:"—"}</b></div><div><small>最差年度</small><b>{worst?`${worst.year} ${(worst.return*100).toFixed(1)}%`:"—"}</b></div></section><section className="card"><h2>資產成長</h2><div className="chart">{sim.length?<ResponsiveContainer width="100%"height="100%"><LineChart data={sim}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="year"/><YAxis/><Tooltip formatter={(v:any)=>[`${fmt(v)} 萬`,"資產"]}/><Line dataKey="value"strokeWidth={3}dot={false}/></LineChart></ResponsiveContainer>:<div className="empty">目前年份範圍沒有可用資料。請調整年份。</div>}</div></section><section className="card"><h2>年度明細</h2><div className="table">{sim.length?<table><thead><tr><th>年份</th><th>年度報酬</th><th>年初資產</th><th>投資損益</th><th>年末資產</th></tr></thead><tbody>{sim.map(r=><tr><td>{r.year}</td><td className={r.return>=0?"up":"down"}>{(r.return*100).toFixed(2)}%</td><td>{fmt(r.before)}</td><td>{r.gain>=0?"+":""}{fmt(r.gain)}</td><td><b>{fmt(r.value)}</b></td></tr>)}</tbody></table>:<div className="empty">沒有資料</div>}</div></section><p className="disclaimer">歷史回測不代表未來報酬，也不是投資建議。價格報酬、含股息總報酬、幣別與匯率會造成不同結果。正式版會在資料頁標明口徑。</p></main></>}
createRoot(document.getElementById("root")!).render(<App/>);